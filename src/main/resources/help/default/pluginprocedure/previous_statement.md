Procedural blocks will need two fields, "previousStatement" and "nextStatement".

This will indicate if the block can have other procedural blocks before it (which is always the case, so you need to set its value to null).