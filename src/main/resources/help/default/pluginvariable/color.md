The color for built-in procedure blocks (e.g. get and set). it can use both HUE (0-360) or hexadecimal values (#FFFFFF) as a String

Default HSV [Saturation=45, Value=65]. If you use the special saturation and value, plugin will omit them and only give the hue.