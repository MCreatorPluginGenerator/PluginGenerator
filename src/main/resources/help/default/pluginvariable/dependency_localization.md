This parameter will be used before the procedure parameter name.

eg. in the "Number parameter x", the "Number parameter" is the value.

Localization key: blockly.block.custom_dependency_${registryname}