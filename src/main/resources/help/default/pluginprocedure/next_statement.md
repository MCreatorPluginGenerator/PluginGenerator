Procedural blocks will need two fields, "previousStatement" and "nextStatement". 

This will indicate if new procedural blocks can be added after this block. This parameter is set to false with the "Cancel action" block.